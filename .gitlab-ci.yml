stages:
  - test
  - deploy

tests:
  stage: test
  script:
    - python3 manage.py test

deploy:
  stage: deploy
  script:
    - cp $ENV .env
    - bash deploy.sh
  only:
    - main
  tags:
    - habit-app
