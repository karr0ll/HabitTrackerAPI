stages:
  - test
  - deploy

tests:
  stage: test
  script:
    - python3 manage.py test

deploy:
  stage: deploy
  script:
    - cp $ENV_DEV .env
    - bash deploy.sh
  #environment: production
  only:
    - main
  tags:
    - habit-app
